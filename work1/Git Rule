규칙을 정하라고 하셔서 규칙을 세워봅니다.

main 브랜치에서 코드를 수정할 때에 temp1 브랜치를 만들어서 수정한 뒤

그 코드가 좋고 올바른 코드이면 그때 가서 메인 브랜치에 병합을 한다.

만약 메인 브랜치와 temp1 브랜치를 같이 수정하고 병합을 할려면 머리가 아파지므로 위의 방식을 숙지한다.

명령어는 gitubcommand.txt 문서를 참조